apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cicd.fullname" . }}-test-connection"
  labels:
    {{- include "cicd.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cicd.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
